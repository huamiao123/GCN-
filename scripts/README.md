# Script roles

Authority scripts:

- `build_extension.sh`
- `run_final_pre_numa_gate.sh`
- `run_final_pre_numa_authority_matrix.sh`
- `validate_authority_plan.py`
- `summarize_authority_cell.py`
- `tfs_standard_env.sh`

Every other launcher in this directory is historical reproduction or an
isolated candidate probe.  It must not be used to produce the final paper
denominator or numerator.
